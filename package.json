{
  "name": "relay-workspace",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently \"pnpm:dev:*\" --names \"worker,react\" --prefix-colors \"blue,magenta\"",
    "dev:worker": "pnpm --filter relay-examples dev",
    "dev:react": "pnpm --filter relay-web dev",
    "dev:e2e": "concurrently \"pnpm:dev:e2e:*\" --names \"worker,react\" --prefix-colors \"blue,magenta\"",
    "dev:e2e:worker": "pnpm --filter relay-e2e-tests dev",
    "dev:e2e:react": "RELAY_WORKER_URL=http://localhost:8788 pnpm --filter relay-web dev --port 5174",
    "build": "pnpm -r build",
    "deploy:all": "pnpm --filter relay-web run deploy && pnpm --filter relay-examples run deploy",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm --filter relay-sdk test",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:install": "playwright install chromium",
    "format": "oxfmt",
    "format:check": "oxfmt --check",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "prepare": "lefthook install",
    "===== PACKAGES =====": "",
    "examples": "pnpm --filter relay-examples",
    "web": "pnpm --filter relay-web",
    "sdk": "pnpm --filter relay-sdk"
  },
  "dependencies": {
    "relay-sdk": "workspace:*"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "concurrently": "^9.2.1",
    "lefthook": "^2.1.1",
    "oxfmt": "^0.35.0",
    "oxlint": "^1.50.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "lefthook"
    ]
  }
}
